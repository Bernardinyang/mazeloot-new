#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run frontend tests before push
# Skip with: git push --no-verify

echo "Running frontend tests before push..."

cd "$(dirname "$0")/.."
npm run test
